% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{us_lifetable_models}
\alias{us_lifetable_models}
\title{Fitted US life-table hazard model parameters}
\format{
A data frame with 27 rows and 6 columns:
\describe{
\item{vintage}{character. One of \code{"table84"}, \code{"table2008"},
\code{"table2023"}. Vintages are not interchangeable: model \emph{structure}
differs, not only fitted values.}
\item{stratum}{character. One of \code{"all"}, \code{"f"}, \code{"m"}, \code{"w"}, \code{"wf"},
\code{"wm"}, plus the vintage's non-white codes -- \code{"o"}, \code{"of"}, \code{"om"} for
\code{table84}; \code{"b"}, \code{"bf"}, \code{"bm"} for \code{table2008} and \code{table2023}.}
\item{params}{list column of named numeric vectors, length 11:
\code{DELTA}, \code{THALF}, \code{NU}, \code{M}, \code{TAU}, \code{GAMMA}, \code{ALPHA}, \code{ETA}, \code{E0},
\code{C0}, \code{L0}.}
\item{status}{list column of named integer vectors, length 11. The
\verb{_STATUS_} gate. \code{mu = exp(E0 | C0 | L0)} \strong{only} where the
corresponding status is \code{1}; status \code{0} means the phase is absent
from the model, not that \code{log(mu)} is zero.}
\item{flags}{list column of named numeric vectors, length 6:
\code{G1FLAG}, \code{FIXDEL0}, \code{FIXMNU1}, \code{G3FLAG}, \code{FIXGE2}, \code{FIXGAE2}. These
record which sub-form was fitted. They are metadata only and are never
read during evaluation.}
\item{vcov}{list column of 11 by 11 numeric matrices, the parameter
covariance blocks. Nothing in this version reads them; they ship
because they are the only route to a confidence band later.}
}
}
\source{
Fitted by The Cleveland Clinic Foundation. Built into this package
by \code{data-raw/build-models.R}, which is not run at install time. That
script's header names the share location to restore the inputs from —
deliberately kept out of \verb{R/}, which must carry no literal share path.
}
\usage{
us_lifetable_models
}
\description{
The fitted three-phase parametric hazard models that the Cleveland Clinic
SAS macro \verb{\%usmatchd} evaluates, one record per vintage per stratum. These
are fitted models, not life tables: \verb{\%usmatchd} runs \verb{PROC HAZPRED} against
a stored fit on the \strong{age} axis, with time origin at birth.
}
\section{Race semantics}{

\code{table84} names its non-white strata \code{o}, honestly "other". \code{table2008}
and \code{table2023} name the same strata \code{b}, and the macro's own comment
asserts these are Black-race estimates. For 2023 that assertion is
\strong{false}: the category is a risk-weighted average of Black, Asian,
American Indian and Hispanic death rates, weighted by number at risk,
stored under \code{B} only to keep the macro's naming consistent. See
\code{\link[=us_lifetable_vintages]{us_lifetable_vintages()}}, which reports this per vintage.
}

\keyword{datasets}
